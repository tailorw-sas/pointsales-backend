apiVersion: apps/v1
kind: Deployment
metadata:
  name: config
  namespace: turnero
spec:
  replicas: 1
  selector:
    matchLabels:
      workload.user.cattle.io/workloadselector: apps.deployment-medinec-config
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: config
        workload.user.cattle.io/workloadselector: apps.deployment-medinec-config
    spec:
      containers:
        - name: config
          image: kmontes2020/medinec-config:v1
          imagePullPolicy: Always
          ports:
            - containerPort: 8081
              protocol: TCP
          envFrom:
            - secretRef:
                name: config-secret
      imagePullSecrets:
        - name: dokcer-hub
      restartPolicy: Always