apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudbridges
  namespace: turnero
spec:
  replicas: 1
  selector:
    matchLabels:
      workload.user.cattle.io/workloadselector: apps.deployment-medinec-cloudbridges
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: cloudbridges
        workload.user.cattle.io/workloadselector: apps.deployment-medinec-cloudbridges
    spec:
      containers:
        - name: cloudbridges
          image: kmontes2020/medinec-cloud-bridges:v1
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: cloudbridges-secret
      imagePullSecrets:
        - name: dokcer-hub
      restartPolicy: Always